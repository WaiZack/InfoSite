<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
  <meta name="description" content="" />
  <meta name="author" content="" />
  <%= stylesheet_link_tag 'application' %>
  <%= stylesheet_link_tag 'dash' %>


  <%= javascript_include_tag ('application') %>

</head>
<body>
<div class="navbar navbar-inverse set-radius-zero">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

    </div>
  </div>
</div>
<!-- LOGO HEADER END-->
<section class="menu-section">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <div class="navbar-collapse collapse ">
          <ul id="menu-top" class="nav navbar-nav navbar-right">
            <li><a href="/dash">Profile</a></li>
            <li><a class="menu-top-active"  href="/teams">Teams</a></li>
            <li>
              <a href=<%= new_password_reset_path%>>
                <p>Change Password</p>
              </a>
            </li>
            <li>
              <%= link_to "Log out", logout_path, method: "delete" %>
            </li>

          </ul>
        </div>
      </div>

    </div>
  </div>
</section>
<!-- MENU SECTION END-->
<div class="content-wrapper">
  <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <h4 class="page-head-line">Team</h4>
      </div>
    </div>
    <div class="row">
      <div class="col-md-4 col-sm-4 col-xs-6">
        <div class="dashboard-div-wrapper bk-clr-two">
          <h5>Team Name</h5>
          <i  class="fa fa-id-card dashboard-div-icon" ></i>
          <h4><%=@team.name%></h4>
        </div>

      </div>
      <div class="col-md-4 col-sm-4 col-xs-6">
        <div class="dashboard-div-wrapper bk-clr-three">
          <h5>Team Track</h5>
          <i  class="fa fa-road dashboard-div-icon" ></i>
          <h4><%=@team.track%></h4>
        </div>

      </div>
      <div class="col-md-4 col-sm-4 col-xs-6">
        <div class="dashboard-div-wrapper bk-clr-four">
          <h5>Team Size</h5>
          <i  class="fa fa-users dashboard-div-icon" ></i>
          <h4><%=@team.users.count%> / 3</h4>
        </div>

      </div>
    </div>

    <div class = "row">
      <div class="table-responsive">
        <table class="table">
          <thead>
          <tr>
            <th>Members</th>
          </tr>

          </thead>

          <% @team.users.all.each do |member| %>
              <tr>
                <td><%= member.email%></td>

                  <% if member.id == @team.creator%>
                    <td class="alert-warning">
                      <i class="fa fa-hand-spock-o" aria-hidden="true"></i> Leader
                    </td>
                  <%end%>


              </tr>
          <%end%>
        </table>
      </div>
    </div>

    <div class="row">
      <% if @team.users.count < 3 && !@team.users.all.include?(@user) && @user.teams.count < 3%>
          <!--<%= simple_form_for @request do |f| %>-->
              <!--<%= f.input :requester_id,input_html: { value: @user.id }%>-->
              <!--<%= f.button :button, 'Request Membership', class: 'btn' %>-->
          <!--<% end %>-->

        <a class="btn btn-primary" href='/membership_requests/new?team_id=<%=@team.id%>'>Send Join Request</a>
      <% end %>
    </div>
  </div>
</div>

</body>
</html>
